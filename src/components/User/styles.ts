
import styled from "styled-components";
import {Theme} from "../../styles";

// export const StyledUser = styled.a< {user: User } >`
//     padding: 0;
//     href: ${(props) => "/user/"+props.user.id};
//     :hover {color: ${Theme.colors.pink};};
// `;
//
